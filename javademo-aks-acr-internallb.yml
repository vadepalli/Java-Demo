apiVersion: apps/v1
kind: Deployment
metadata:
  name: javademointernal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: javademointernal
  template:
    metadata:
      labels:
        app: javademointernal
    spec:
      containers:
      - name: javademointernal
        image: ckacr001.azurecr.cn/javademo:294
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
      nodeSelector:
        kubernetes.io/os: linux
---
kind: Service
apiVersion: v1
metadata:
  name: javademointernal
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"  
spec:
  type:
    LoadBalancer
  selector:
    app: javademointernal
  ports:
  - port: 80
    targetPort: 8080

